server:
  port: 8888
spring:  application:    name: config-server  cloud:    config:      server:        git:          uri: https://github.com/akshat0808/
          search-paths:          - 'configuration-files/{application}'